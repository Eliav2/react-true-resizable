---
sidebar_position: 1
---


# Quick Start

## Install

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

[//]: # (@formatter:off)

<Tabs>
  <TabItem value="yarn" label="yarn">

```bash
yarn add react-true-resizable
```

  </TabItem>
  <TabItem value="pnpm" label="pnpm">

```bash
pnpm add react-true-resizable
```

  </TabItem>
  <TabItem value="npm" label="npm">

```bash
npm install react-true-resizable
```

  </TabItem>
</Tabs>

[//]: # (temp-prettier bug)
[//]: # (@formatter:on)


## Main Features

- Resize any DOM node.
- Does not wrap the target DOM node with other DOM elements.
- Does not rely on the target DOM node CSS positioning style(and does not change it).
- Written in modern React techniques and works great with other modern React UI frameworks like MUI.
- Handles correctly and predictably refs forwarding behaviors.
- Simple and efficient.
- Simple API, yet can be easily customized.

in simple words - it will work as you would expect.

:::info Why
See [comparison](/docs/comparison) with other Resizable solutions to learn why this lib is different.
:::

## Basic Usage

import DemoPreviewer from '@site/src/components/DemoPreviewer';

import Basic from '@site/src/demos/Basic';

<DemoPreviewer name={"Basic"} Comp={Basic}/>

## Live Edit
```jsx live
function Live () {
  const BoxStyle = {
    border: "solid",
    borderRadius: 12,
    width: 120,
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
  };
  return (
    <Resizable>
      <div style={BoxStyle}>Resizable</div>
    </Resizable>
  );
}
```

